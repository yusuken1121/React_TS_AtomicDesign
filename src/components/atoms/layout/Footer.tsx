import { FC } from "react";

export const Footer: FC = () => {
  return (
    <footer className="flex items-center justify-center w-full h-24 bg-slate-500 fixed bottom-0">
      <p className="bold text-white">@copy right</p>
    </footer>
  );
};
